---
phase: 05-polish-and-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OlympeMaterialManager/src/OlympeMaterialManager/Themes/OlympeTheme.xaml
  - OlympeMaterialManager/src/OlympeMaterialManager/Views/LeftPanelView.xaml
  - OlympeMaterialManager/src/OlympeMaterialManager/Views/CenterPanelView.xaml
  - OlympeMaterialManager/src/OlympeMaterialManager/Views/RightPanelView.xaml
  - OlympeMaterialManager/src/OlympeMaterialManager/Views/AddMaterialDialog.xaml
  - OlympeMaterialManager/src/OlympeMaterialManager/Views/MainWindow.xaml
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "All ContextMenus render with dark Olympe theme (SurfaceBrush background, not Windows default white)"
    - "All MenuItems show accent highlight on hover, 50% opacity when disabled"
    - "ToolTips render with dark background and rounded corners matching the Olympe design"
    - "CheckBox controls display custom dark-themed box with accent checkmark"
    - "Every interactive control has visible hover, focus (accent border), and disabled (50% opacity) states"
    - "No control in any View uses Windows default light theme appearance"
  artifacts:
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Themes/OlympeTheme.xaml"
      provides: "Complete set of implicit styles for all WPF controls used in the application"
      contains: "ContextMenu"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Themes/OlympeTheme.xaml"
      provides: "CheckBox, MenuItem, ToolTip, Separator styles"
      contains: "CheckBox"
  key_links:
    - from: "OlympeTheme.xaml"
      to: "All Views"
      via: "Implicit style resolution"
      pattern: "TargetType=\"ContextMenu\""
    - from: "Views with local styles"
      to: "OlympeTheme.xaml"
      via: "BasedOn={StaticResource {x:Type ...}}"
      pattern: "BasedOn"
---

<objective>
Complete the Olympe dark theme by adding missing implicit styles for all WPF controls used across the application, and audit all Views for unstyled or partially styled controls.

Purpose: Ensure every WPF control in the application visually belongs to the Olympe dark design system (requirement UI-03). The existing OlympeTheme.xaml has 12 control styles but is missing ContextMenu, MenuItem, ToolTip, CheckBox, Separator, and proper hover/focus/disabled states for all controls.

Output: Extended OlympeTheme.xaml with ~7 additional implicit styles, and all Views updated with BasedOn references where local styles override the theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-installer/05-CONTEXT.md
@.planning/phases/05-polish-and-installer/05-RESEARCH.md

@OlympeMaterialManager/src/OlympeMaterialManager/Themes/OlympeTheme.xaml
@OlympeMaterialManager/src/OlympeMaterialManager/Views/MainWindow.xaml
@OlympeMaterialManager/src/OlympeMaterialManager/Views/LeftPanelView.xaml
@OlympeMaterialManager/src/OlympeMaterialManager/Views/CenterPanelView.xaml
@OlympeMaterialManager/src/OlympeMaterialManager/Views/RightPanelView.xaml
@OlympeMaterialManager/src/OlympeMaterialManager/Views/AddMaterialDialog.xaml

<interfaces>
<!-- Existing Olympe theme color palette (do NOT change these -- extend only) -->
From OlympeTheme.xaml:
- BackgroundColor: #1E1E2E
- SurfaceColor: #2D2D3D
- AccentColor: #FF9800
- AccentHoverColor: #FFA726
- TextPrimaryColor: #E0E0E0
- TextSecondaryColor: #A0A0A0
- BorderColor: #3D3D4D
- ErrorColor: #EF5350

Existing brushes: BackgroundBrush, SurfaceBrush, AccentBrush, AccentHoverBrush, TextPrimaryBrush, TextSecondaryBrush, BorderBrush, ErrorBrush

Already styled (12): Window (keyed), Button, TextBlock, TextBox, ComboBox, ComboBoxItem, ListBox, ListBoxItem, TreeView, TreeViewItem, ScrollBar, GridSplitter, SetMatButton (keyed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing implicit styles to OlympeTheme.xaml</name>
  <files>OlympeMaterialManager/src/OlympeMaterialManager/Themes/OlympeTheme.xaml</files>
  <action>
Read OlympeTheme.xaml fully. Then add the following implicit styles, each with a full ControlTemplate following the established pattern (Border with CornerRadius=4, TemplateBinding for Background/BorderBrush/Padding, triggers for hover/focus/disabled states per D-03).

Add these implicit styles AFTER the existing SetMatButtonStyle, in this order:

1. **ContextMenu** (per D-01): SurfaceBrush background, BorderBrush border, CornerRadius=4, StackPanel as ItemsHost. ContextMenu renders in a Popup so must have its own complete style.

2. **MenuItem** (per D-01): Transparent background, TextPrimaryBrush foreground, Padding 8,4. ControlTemplate with Border + ContentPresenter (ContentSource="Header"). Triggers: IsHighlighted -> SurfaceBrush background (slightly lighter than context menu). IsEnabled=False -> Opacity 0.5.

3. **ToolTip** (per D-01): SurfaceBrush background, BorderBrush border, CornerRadius=4, Padding 8,4. ControlTemplate with Border + ContentPresenter.

4. **CheckBox** (per D-01): Custom ControlTemplate with a StackPanel (Horizontal) containing: (a) an 18x18 Border (SurfaceBrush background, BorderBrush border, CornerRadius=3) with a Path checkmark inside (Data="M 2 6 L 6 10 L 14 2", AccentBrush stroke, StrokeThickness=2, Visibility=Collapsed), and (b) a ContentPresenter with Margin="6,0,0,0". Triggers: IsChecked=True -> show checkmark + accent border. IsMouseOver -> accent border. IsEnabled=False -> Opacity 0.5. Per D-03, add IsFocused trigger -> accent border.

5. **Separator** (per D-01): Height=1, Background=BorderBrush, Margin="4,2". Simple ControlTemplate with a Border.

6. **GroupBox** (per D-01): SurfaceBrush background, BorderBrush border, CornerRadius=4. ControlTemplate with a Grid: Row 0 = Header (TextPrimaryBrush, FontWeight=SemiBold), Row 1 = ContentPresenter. Border wraps the whole thing.

7. **Expander** (per D-01): SurfaceBrush background, TextPrimaryBrush foreground. ControlTemplate with Grid: Row 0 = ToggleButton header (reuse the arrow Path pattern from TreeViewItem expander, rotates on expand), Row 1 = ContentPresenter (Visibility bound to IsExpanded). Triggers: IsMouseOver on header -> AccentBrush arrow.

For each ControlTemplate, ensure ALL three interaction states per D-03:
- IsMouseOver=True -> accent visual feedback (border or background highlight)
- IsFocused=True -> AccentBrush border (for focusable controls: CheckBox, Expander toggle)
- IsEnabled=False -> Opacity=0.5

Use the research-provided code examples as reference but adapt to match the EXACT existing color keys and pattern (SurfaceBrush for elevated surfaces, BackgroundBrush for recessed areas, AccentBrush for highlights, CornerRadius=4 throughout).
  </action>
  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET TEXTURE MANAGER/OlympeMaterialManager" && dotnet build src/OlympeMaterialManager/OlympeMaterialManager.csproj --no-restore -v q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>OlympeTheme.xaml contains implicit styles for ContextMenu, MenuItem, ToolTip, CheckBox, Separator, GroupBox, and Expander. Each style has a ControlTemplate with hover, focus (where applicable), and disabled triggers. Build succeeds for both TFMs.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Views and fix unstyled controls</name>
  <files>
    OlympeMaterialManager/src/OlympeMaterialManager/Views/LeftPanelView.xaml
    OlympeMaterialManager/src/OlympeMaterialManager/Views/CenterPanelView.xaml
    OlympeMaterialManager/src/OlympeMaterialManager/Views/RightPanelView.xaml
    OlympeMaterialManager/src/OlympeMaterialManager/Views/AddMaterialDialog.xaml
    OlympeMaterialManager/src/OlympeMaterialManager/Views/MainWindow.xaml
  </files>
  <action>
Read ALL five View XAML files. For each file, audit per D-02:

1. **Find local styles that override theme styles WITHOUT BasedOn.** Any `<Style TargetType="ListBoxItem">` or similar defined locally in a View MUST include `BasedOn="{StaticResource {x:Type ListBoxItem}}"` to inherit the theme ControlTemplate (CornerRadius, accent left-border, hover/selected triggers). Without BasedOn, the local style completely replaces the implicit theme style, losing the dark theme appearance. The research specifically flags CenterPanelView and AddMaterialDialog as having this anti-pattern.

2. **Find any controls using hardcoded light-theme colors** (e.g., Background="White", Foreground="Black", or any non-Olympe color literal). Replace with StaticResource references to the Olympe brushes.

3. **Find controls missing explicit style references where needed.** For example, if AddMaterialDialog uses WindowStyle=ToolWindow, ensure it still applies Style="{StaticResource OlympeWindowStyle}" so Background and Foreground are themed.

4. **Verify ContextMenu declarations** in LeftPanelView and RightPanelView. Now that ContextMenu has an implicit style, verify the ContextMenus pick it up. If any ContextMenu has an inline style, add BasedOn to the implicit ContextMenu style.

For each fix, add a comment like `<!-- UI-03: BasedOn added for theme consistency -->` next to the change so the reviewer can trace it.

Do NOT change any ViewModel bindings, command wiring, or layout structure. Only fix styling/theming issues.
  </action>
  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET TEXTURE MANAGER/OlympeMaterialManager" && dotnet build src/OlympeMaterialManager/OlympeMaterialManager.csproj --no-restore -v q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All Views use themed controls exclusively. Every local style that overrides a theme style uses BasedOn to inherit the ControlTemplate. No hardcoded light-theme colors remain. Build succeeds for both TFMs.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `dotnet build OlympeMaterialManager.sln -c Release -v q` succeeds with zero errors for both net48 and net8.0-windows
2. OlympeTheme.xaml contains implicit styles for: ContextMenu, MenuItem, ToolTip, CheckBox, Separator, GroupBox, Expander (grep for each TargetType)
3. No View XAML contains a local Style without BasedOn (grep for `<Style TargetType=` in Views directory and verify each has BasedOn or is the only style for that type)
</verification>

<success_criteria>
- OlympeTheme.xaml has 19+ control styles (12 existing + 7 new)
- Every interactive control style includes IsMouseOver, IsFocused (where applicable), and IsEnabled=False triggers
- All View XAML local styles use BasedOn to inherit theme ControlTemplates
- Solution builds successfully for both TFMs with zero XAML errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-installer/05-01-SUMMARY.md`
</output>
