---
phase: 03-preset-panel-and-set-mat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetMaterialDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetGroupDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetCollectionDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/SetMatRequestDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/SetMatParamRequestDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/DuplicateMaterialRequestDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Models/AppSettingsDto.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Services/PresetService.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Services/DialogService.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Converters/ArgbToColorConverter.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Messages/RefreshLayersMessage.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitRequestType.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitEventBridge.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/ViewModels/CenterPanelViewModel.cs
  - OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj
autonomous: true
requirements:
  - PRESET-01
  - PRESET-02
  - PRESET-04
  - PRESET-05
  - PRESET-06
  - PRESET-07
  - PRESET-08
  - PRESET-09
  - PRESET-10

must_haves:
  truths:
    - "DTOs define the preset data model contracts for all downstream plans"
    - "PresetService can Load/Save JSON preset files and manage settings path"
    - "RevitEventBridge handles 4 new operations: GetAllMaterials, SetMaterialOnLayers, SetMaterialOnParameter, DuplicateMaterial"
    - "CenterPanelViewModel exposes CurrentTypeIdValue and handles RefreshLayersMessage"
  artifacts:
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetMaterialDto.cs"
      provides: "MaterialName, MaterialElementIdValue (long), ColorArgb (int)"
      contains: "class PresetMaterialDto"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetGroupDto.cs"
      provides: "GroupName, ObservableCollection<PresetMaterialDto>"
      contains: "class PresetGroupDto"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetCollectionDto.cs"
      provides: "Root serialization unit wrapping groups"
      contains: "class PresetCollectionDto"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Services/PresetService.cs"
      provides: "Load, Save, GetStoredPresetPath, StorePresetPath, GetDefaultCollection"
      contains: "class PresetService"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitEventBridge.cs"
      provides: "4 new handler methods"
      contains: "HandleGetAllMaterials"
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitRequestType.cs"
      provides: "4 new enum values"
      contains: "GetAllMaterials"
  key_links:
    - from: "Services/PresetService.cs"
      to: "Models/PresetCollectionDto.cs"
      via: "System.Text.Json serialization"
      pattern: "JsonSerializer\\.Serialize|JsonSerializer\\.Deserialize"
    - from: "Events/RevitEventBridge.cs"
      to: "Models/SetMatRequestDto.cs"
      via: "handler parameter casting"
      pattern: "SetMatRequestDto"
    - from: "ViewModels/CenterPanelViewModel.cs"
      to: "Messages/RefreshLayersMessage.cs"
      via: "WeakReferenceMessenger registration"
      pattern: "Register<RefreshLayersMessage>"
---

<objective>
Create all data contracts (DTOs), the PresetService for JSON persistence, the DialogService for folder browsing, 4 new RevitEventBridge handlers (GetAllMaterials, SetMaterialOnLayers, SetMaterialOnParameter, DuplicateMaterial), the ArgbToColorConverter, and extend CenterPanelViewModel with CurrentTypeIdValue and RefreshLayersMessage support.

Purpose: Establish the complete data layer and Revit API write operations that Plan 02 (right panel UI) and Plan 03 (Set Mat coordination) depend on. No UI in this plan -- pure models, services, and bridge handlers.

Output: 15 files (7 new DTOs, 2 services, 1 converter, 1 message, 2 modified Events files, 1 modified ViewModel, 1 modified csproj)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preset-panel-and-set-mat/03-CONTEXT.md
@.planning/phases/03-preset-panel-and-set-mat/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from the existing codebase. -->

From Events/RevitEventBridge.cs (existing pattern to extend):
```csharp
public class RevitEventBridge
{
    public void MakeRequest(RevitRequestType type, object? data, Action<object?> callback) { ... }
    public void ProcessRequest(UIApplication uiApp) { ... }
    // Switch cases for existing handlers -- add new cases here
}
```

From Events/RevitRequestType.cs (add 4 new values after existing):
```csharp
public enum RevitRequestType
{
    None,
    GetDocumentInfo,
    GetFamilyList,
    GetTypeList,
    GetLayersForType,
    GetMaterialParametersForType,
    // Phase 3 additions go here
}
```

From Models/LayerDto.cs (needed by SetMat -- read LayerIndex):
```csharp
public class LayerDto
{
    public int LayerIndex { get; set; }
    public string Function { get; set; } = string.Empty;
    public double Width { get; set; }
    public string MaterialName { get; set; } = string.Empty;
    public long MaterialElementIdValue { get; set; } = -1;
}
```

From Models/MaterialParamDto.cs (needed by SetMat -- read ParameterDefinitionName):
```csharp
public class MaterialParamDto
{
    public string ParameterName { get; set; } = string.Empty;
    public string CurrentMaterialName { get; set; } = string.Empty;
    public long CurrentMaterialIdValue { get; set; } = -1;
    public string ParameterDefinitionName { get; set; } = string.Empty;
}
```

From Helpers/ElementIdHelper.cs (used in all handlers):
- `ElementIdHelper.FromValue(long)` -- creates ElementId from long
- `ElementIdHelper.GetValue(ElementId)` -- extracts long from ElementId

From Converters/BoolToVisibilityConverter.cs (existing pattern for new converter):
- Standard IValueConverter pattern in Olympe.MaterialManager.Converters namespace

From ViewModels/CenterPanelViewModel.cs (extend with CurrentTypeIdValue + RefreshLayersMessage):
```csharp
public partial class CenterPanelViewModel : ObservableObject
{
    private readonly RevitEventBridge? _eventBridge;
    // ... existing properties
    [ObservableProperty] private IList? _selectedItems;
    // Existing methods: OnTypeSelected, FetchLayers, FetchMaterialParameters
    // Add: CurrentTypeIdValue property, RefreshLayersMessage handler registration
}
```

Conditional compilation constants:
- `REVIT2024` for net48
- `REVIT2025_OR_GREATER` for net8.0-windows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DTOs, Services, Converter, Message, and add System.Text.Json NuGet</name>
  <files>
    OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetMaterialDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetGroupDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/PresetCollectionDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/SetMatRequestDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/SetMatParamRequestDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/DuplicateMaterialRequestDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Models/AppSettingsDto.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Services/PresetService.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Services/DialogService.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Converters/ArgbToColorConverter.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Messages/RefreshLayersMessage.cs
    OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj
  </files>
  <action>
    **read_first:** Read OlympeMaterialManager.csproj, Models/LayerDto.cs (for DTO style), Converters/BoolToVisibilityConverter.cs (for converter style), Messages/TypeSelectedMessage.cs (for message style), Helpers/ElementIdHelper.cs

    **1. Create DTOs per D-01, D-02 (7 files):**

    - `PresetMaterialDto.cs`: POCO in `Olympe.MaterialManager.Models` namespace. Properties: `MaterialName` (string, default ""), `MaterialElementIdValue` (long, default -1), `ColorArgb` (int, default 0). No Revit imports. Follow existing DTO style (no ObservableObject -- pure POCO for serialization).

    - `PresetGroupDto.cs`: POCO. Properties: `GroupName` (string, default ""), `Materials` (ObservableCollection&lt;PresetMaterialDto&gt;, initialized in constructor to new()). Note: ObservableCollection for UI binding but still serializable by System.Text.Json.

    - `PresetCollectionDto.cs`: POCO. Property: `Groups` (ObservableCollection&lt;PresetGroupDto&gt;, initialized to new()). This is the root serialization unit per D-02.

    - `SetMatRequestDto.cs`: POCO. Properties: `TargetTypeIdValue` (long), `LayerIndices` (int[]), `MaterialIdValue` (long). Per D-22.

    - `SetMatParamRequestDto.cs`: POCO. Properties: `TargetTypeIdValue` (long), `MaterialIdValue` (long), `ParameterDefinitionNames` (string[]). Batches multiple parameter names in a single request per Research open question 2.

    - `DuplicateMaterialRequestDto.cs`: POCO. Properties: `MaterialIdValue` (long). Per D-23.

    - `AppSettingsDto.cs`: POCO. Property: `PresetFilePath` (string?, default null). Per D-05.

    **2. Create Services/PresetService.cs per D-04, D-05, D-06:**

    Create `Services/` directory. Implement `PresetService` in `Olympe.MaterialManager.Services` namespace:
    - Private static `JsonSerializerOptions` with `WriteIndented = true`, `PropertyNamingPolicy = JsonNamingPolicy.CamelCase`.
    - Constructor: compute `_settingsPath` as `%APPDATA%/Olympe/MaterialManager/settings.json`, call `Directory.CreateDirectory` on the parent dir.
    - `GetStoredPresetPath()`: returns string? -- reads settings.json, deserializes AppSettingsDto, returns PresetFilePath. Returns null if file not found or parse error.
    - `StorePresetPath(string path)`: serializes AppSettingsDto to settings.json.
    - `Load(string path)`: reads JSON, deserializes PresetCollectionDto. Returns GetDefaultCollection() on error. Wrap in try/catch for IOException, JsonException.
    - `Save(PresetCollectionDto collection, string path)`: serializes to JSON, writes to file.
    - `GetDefaultCollection()`: static method, returns PresetCollectionDto with 3 default groups per D-03: "Murs", "Sols", "Autres" (each with empty Materials collection).

    Follow the exact pattern from RESEARCH Pattern 5. Use `System.Text.Json` (not Newtonsoft).

    **3. Create Services/DialogService.cs per D-04:**

    Static class with `ShowFolderBrowser(string title)` returning `string?`. Use conditional compilation:
    - `#if REVIT2025_OR_GREATER`: use `Microsoft.Win32.OpenFolderDialog` (set Title, call ShowDialog).
    - `#else` (net48): use `System.Windows.Forms.FolderBrowserDialog` (set Description, ShowNewFolderButton=true, call ShowDialog, check DialogResult.OK).
    Follow exact pattern from RESEARCH Pattern 4.

    **4. Create Converters/ArgbToColorConverter.cs:**

    IValueConverter in `Olympe.MaterialManager.Converters` namespace. Follow same style as existing BoolToVisibilityConverter.
    - Convert: if value is int argb, create `System.Drawing.Color.FromArgb(argb)`, return `System.Windows.Media.Color.FromArgb(a, r, g, b)`. Fallback: return `Colors.Gray`.
    - ConvertBack: throw NotSupportedException.

    **5. Create Messages/RefreshLayersMessage.cs per D-25:**

    Follow existing TypeSelectedMessage pattern. Class `RefreshLayersMessage` extending `ValueChangedMessage<long>` from CommunityToolkit.Mvvm.Messaging. Constructor takes `long typeIdValue` and passes to base. In `Olympe.MaterialManager.Messages` namespace.

    **6. Add System.Text.Json NuGet to csproj:**

    Add `<PackageReference Include="System.Text.Json" Version="8.0.5" />` in the shared (unconditional) ItemGroup of OlympeMaterialManager.csproj. Also add conditional System.Windows.Forms reference for net48 folder dialog:
    ```xml
    <ItemGroup Condition="'$(TargetFramework)' == 'net48'">
        <Reference Include="System.Windows.Forms" />
    </ItemGroup>
    ```

    **acceptance_criteria:**
    - grep "class PresetMaterialDto" Models/PresetMaterialDto.cs
    - grep "class PresetGroupDto" Models/PresetGroupDto.cs
    - grep "class PresetCollectionDto" Models/PresetCollectionDto.cs
    - grep "class SetMatRequestDto" Models/SetMatRequestDto.cs
    - grep "class SetMatParamRequestDto" Models/SetMatParamRequestDto.cs
    - grep "class DuplicateMaterialRequestDto" Models/DuplicateMaterialRequestDto.cs
    - grep "class AppSettingsDto" Models/AppSettingsDto.cs
    - grep "class PresetService" Services/PresetService.cs
    - grep "class DialogService" Services/DialogService.cs
    - grep "class ArgbToColorConverter" Converters/ArgbToColorConverter.cs
    - grep "class RefreshLayersMessage" Messages/RefreshLayersMessage.cs
    - grep "System.Text.Json" OlympeMaterialManager.csproj
  </action>
  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET TEXTURE MANAGER" && dotnet build OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>12 new files created (7 DTOs, 2 services, 1 converter, 1 message), csproj updated with System.Text.Json 8.0.5 and conditional System.Windows.Forms reference. Build succeeds on all TFMs.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 RevitEventBridge handlers and extend CenterPanelViewModel</name>
  <files>
    OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitRequestType.cs
    OlympeMaterialManager/src/OlympeMaterialManager/Events/RevitEventBridge.cs
    OlympeMaterialManager/src/OlympeMaterialManager/ViewModels/CenterPanelViewModel.cs
  </files>
  <action>
    **read_first:** Read Events/RevitEventBridge.cs (full), Events/RevitRequestType.cs (full), ViewModels/CenterPanelViewModel.cs (full), Models/SetMatRequestDto.cs (just created), Models/SetMatParamRequestDto.cs (just created), Models/DuplicateMaterialRequestDto.cs (just created), Models/PresetMaterialDto.cs (just created)

    **1. Extend RevitRequestType.cs per D-20:**

    Add 4 new enum values after the existing Phase 2 values:
    ```
    GetAllMaterials,              // Phase 3 : data=null, returns List<PresetMaterialDto>
    SetMaterialOnLayers,          // Phase 3 : data=SetMatRequestDto, returns null (success) or Exception
    SetMaterialOnParameter,       // Phase 3 : data=SetMatParamRequestDto, returns null (success) or Exception
    DuplicateMaterial,            // Phase 3 : data=DuplicateMaterialRequestDto, returns PresetMaterialDto
    ```

    **2. Extend RevitEventBridge.ProcessRequest switch per D-20:**

    Add 4 new case branches in the existing switch statement:
    - `GetAllMaterials` -> `HandleGetAllMaterials(uiApp)`
    - `SetMaterialOnLayers` -> `HandleSetMaterialOnLayers(uiApp, (SetMatRequestDto)data!)`
    - `SetMaterialOnParameter` -> `HandleSetMaterialOnParameter(uiApp, (SetMatParamRequestDto)data!)`
    - `DuplicateMaterial` -> `HandleDuplicateMaterial(uiApp, (DuplicateMaterialRequestDto)data!)`

    **3. Implement HandleGetAllMaterials per D-21:**

    Returns `List<PresetMaterialDto>`. Use `FilteredElementCollector(doc).OfClass(typeof(Material))` (native quick filter), then `.Cast<Material>()`, then project to PresetMaterialDto with MaterialName, MaterialElementIdValue (via ElementIdHelper.GetValue), ColorArgb (use `m.Color.IsValid` check -- if valid, construct ARGB from 255 alpha + RGB; if invalid, use Gray). Order by MaterialName. Follow RESEARCH code example exactly.

    **4. Implement HandleSetMaterialOnLayers per D-16, D-22:**

    Takes `SetMatRequestDto`. Within a Transaction named "Olympe : Appliquer materiau aux couches":
    - Get element as HostObjAttributes. Throw French InvalidOperationException if null.
    - `GetCompoundStructure()` -- CRITICAL: returns a COPY. Throw if null.
    - Loop over `request.LayerIndices`, call `cs.SetMaterialId(layerIndex, matId)` for each.
    - Call `hostAttrs.SetCompoundStructure(cs)` to PERSIST changes.
    - `tx.Commit()`. In catch: `tx.RollBack()` if started and not ended, then rethrow.
    Follow RESEARCH Pattern 1 exactly. Add comment `// COPY -- must call SetCompoundStructure() to persist` at GetCompoundStructure line.

    **5. Implement HandleSetMaterialOnParameter per D-17:**

    Takes `SetMatParamRequestDto`. Within a Transaction named "Olympe : Appliquer materiau aux parametres":
    - Get element from doc. Get matId from request.
    - Loop over `request.ParameterDefinitionNames`. For each name: `element.LookupParameter(name)` -- throw French error if null or read-only. Call `param.Set(matId)` -- throw French error if returns false.
    - `tx.Commit()`. Catch with RollBack pattern.
    Note: batches all selected parameters in a single Transaction (single undo step) per Research open question 2 recommendation.

    **6. Implement HandleDuplicateMaterial per D-23:**

    Takes `DuplicateMaterialRequestDto`. Within a Transaction named "Olympe : Dupliquer materiau":
    - Get original Material. Throw French error if null.
    - Compute `newName = $"{original.Name} copie"`. Handle name collision by appending counter (copie 2, copie 3...) using FilteredElementCollector check.
    - Call `original.Duplicate(newName)`. IMPORTANT: `Material.Duplicate()` returns the new Material element (Revit API). This is NOT `ElementType.Duplicate()` which returns `ElementType`. For Material specifically, use `Material dup = doc.GetElement(original.Duplicate(newName)) as Material` only if the API returns ElementType. Check RESEARCH: it says `Material.Duplicate(string name)` returns `Material` directly. Use that.
    - Build and return PresetMaterialDto with new name, id, and color.
    Follow RESEARCH Pattern 3. Note: appearance assets are shared by reference -- acceptable for Phase 3 per Research Pitfall 2.

    **7. Add helper method ExtractColorArgb(Material m):**

    Private static method in RevitEventBridge. Returns int (ARGB). Same logic as in HandleGetAllMaterials: if Color.IsValid, build ARGB; else return Gray. Reuse in both HandleGetAllMaterials and HandleDuplicateMaterial.

    **8. Extend CenterPanelViewModel per D-25:**

    - Add `[ObservableProperty] private long _currentTypeIdValue;` -- set in `OnTypeSelected(type)` method: `CurrentTypeIdValue = type.ElementIdValue`.
    - Register `RefreshLayersMessage` in constructor alongside existing TypeSelectedMessage:
      ```csharp
      WeakReferenceMessenger.Default.Register<RefreshLayersMessage>(this, (r, m) =>
      {
          var vm = (CenterPanelViewModel)r;
          if (vm.ShowLayers)
              vm.FetchLayers(m.Value);
          else if (vm.ShowParameters)
              vm.FetchMaterialParameters(m.Value);
      });
      ```
    - Make FetchLayers and FetchMaterialParameters internal (or keep private -- the Messenger handler is inside the class, so private is fine).

    **acceptance_criteria:**
    - grep "GetAllMaterials" Events/RevitRequestType.cs
    - grep "SetMaterialOnLayers" Events/RevitRequestType.cs
    - grep "SetMaterialOnParameter" Events/RevitRequestType.cs
    - grep "DuplicateMaterial" Events/RevitRequestType.cs
    - grep "HandleGetAllMaterials" Events/RevitEventBridge.cs
    - grep "HandleSetMaterialOnLayers" Events/RevitEventBridge.cs
    - grep "SetCompoundStructure" Events/RevitEventBridge.cs
    - grep "HandleDuplicateMaterial" Events/RevitEventBridge.cs
    - grep "CurrentTypeIdValue" ViewModels/CenterPanelViewModel.cs
    - grep "RefreshLayersMessage" ViewModels/CenterPanelViewModel.cs
  </action>
  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET TEXTURE MANAGER" && dotnet build OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>RevitRequestType has 4 new enum values. RevitEventBridge has 4 new handler methods implementing GetAllMaterials, SetMaterialOnLayers (with Get-Modify-Set CompoundStructure pattern), SetMaterialOnParameter (batched single Transaction), and DuplicateMaterial (with name collision handling). CenterPanelViewModel exposes CurrentTypeIdValue and handles RefreshLayersMessage. Build succeeds on all TFMs.</done>
</task>

</tasks>

<verification>
1. All 7 new DTO files exist in Models/ with correct properties per D-01, D-02
2. PresetService.cs in Services/ with Load/Save/GetDefaultCollection methods per D-04, D-05, D-06
3. DialogService.cs in Services/ with conditional compilation per REVIT2024/REVIT2025_OR_GREATER
4. ArgbToColorConverter.cs in Converters/ converts int ARGB to WPF Color
5. RefreshLayersMessage.cs in Messages/ extends ValueChangedMessage&lt;long&gt;
6. RevitRequestType.cs has 4 new enum values per D-20
7. RevitEventBridge.cs has 4 new handler methods with Transaction patterns per D-16, D-17, D-21, D-23
8. CenterPanelViewModel.cs has CurrentTypeIdValue property and RefreshLayersMessage handler
9. System.Text.Json 8.0.5 in csproj
10. Build succeeds on all TFMs (net48, net8.0-windows for 2025, net8.0-windows for 2026)
</verification>

<success_criteria>
- `dotnet build` succeeds with 0 errors on all target frameworks
- All DTOs, services, converter, message, and bridge handlers are complete implementations (no stubs)
- CompoundStructure write uses Get-Modify-Set pattern with explicit SetCompoundStructure() call
- Transaction error handling uses explicit RollBack() in catch blocks
- PresetService defaults to 3 groups: "Murs", "Sols", "Autres" per D-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-preset-panel-and-set-mat/03-01-SUMMARY.md`
</output>
