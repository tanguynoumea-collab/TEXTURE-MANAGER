---
phase: 01-foundation-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OlympeMaterialManager/OlympeMaterialManager.sln
  - OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj
  - OlympeMaterialManager/.gitignore
  - OlympeMaterialManager/addin/OlympeMaterialManager.2024.addin
  - OlympeMaterialManager/addin/OlympeMaterialManager.2025.addin
  - OlympeMaterialManager/addin/OlympeMaterialManager.2026.addin
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-09

must_haves:
  truths:
    - "dotnet build produces assemblies for both net48 and net8.0-windows without errors"
    - "Revit API NuGet packages are referenced with CopyLocal=false (no RevitAPI.dll in output directories)"
    - "Three .addin files exist with correct XML structure, one per Revit version, each pointing to the correct assembly path"
    - "CommunityToolkit.Mvvm, PolySharp, and Microsoft.Xaml.Behaviors.Wpf are restored successfully"
  artifacts:
    - path: "OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj"
      provides: "Multi-target SDK-style csproj with conditional PackageReferences"
      contains: "TargetFrameworks"
    - path: "OlympeMaterialManager/OlympeMaterialManager.sln"
      provides: "Solution file referencing the main project"
    - path: "OlympeMaterialManager/addin/OlympeMaterialManager.2024.addin"
      provides: "Revit 2024 add-in registration"
      contains: "RevitAddIns"
    - path: "OlympeMaterialManager/addin/OlympeMaterialManager.2025.addin"
      provides: "Revit 2025 add-in registration"
      contains: "RevitAddIns"
    - path: "OlympeMaterialManager/addin/OlympeMaterialManager.2026.addin"
      provides: "Revit 2026 add-in registration"
      contains: "RevitAddIns"
  key_links:
    - from: "OlympeMaterialManager.csproj"
      to: "NuGet packages"
      via: "conditional PackageReference per TFM"
      pattern: "Condition.*TargetFramework.*net48"
    - from: ".addin files"
      to: "assembly output"
      via: "Assembly path in XML"
      pattern: "net48.*OlympeMaterialManager.dll"
---

<objective>
Create the solution structure, SDK-style multi-target csproj, and .addin registration files for Olympe MaterialManager. Validate that the build produces assemblies for both net48 (Revit 2024) and net8.0-windows (Revit 2025/2026).

Purpose: This is the critical build-infrastructure gate. If multi-target XAML compilation fails, the fallback to Shared Project must happen before any feature code is written. Per D-01, we attempt single csproj first.
Output: A building solution with NuGet packages restored and .addin manifests ready for deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create solution structure and multi-target csproj</name>
  <files>
    OlympeMaterialManager/OlympeMaterialManager.sln,
    OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj,
    OlympeMaterialManager/.gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md (lines 114-158 for exact csproj XML),
    .planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md (D-01 through D-04 decisions),
    .planning/research/STACK.md (lines 279-312 for Installation Commands)
  </read_first>
  <action>
    1. Create directory structure:
       ```
       OlympeMaterialManager/
         src/
           OlympeMaterialManager/
       ```

    2. Create `OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj` with EXACT content per D-01 and research:
       - `<TargetFrameworks>net48;net8.0-windows</TargetFrameworks>` (per D-01)
       - `<LangVersion>12.0</LangVersion>`
       - `<UseWPF>true</UseWPF>`
       - `<Nullable>enable</Nullable>`
       - `<ImplicitUsings>enable</ImplicitUsings>`
       - `<RootNamespace>Olympe.MaterialManager</RootNamespace>`
       - `<AssemblyName>OlympeMaterialManager</AssemblyName>`
       - Conditional compilation symbols: `REVIT2024` for net48, `REVIT2025_OR_GREATER` for net8.0-windows
       - Shared packages: CommunityToolkit.Mvvm 8.4.2 (per D-15), PolySharp 1.15.0 with PrivateAssets=all (per D-03), Microsoft.Xaml.Behaviors.Wpf 1.1.142
       - net48 conditional: Nice3point.Revit.Api.RevitAPI 2024.3.30, Nice3point.Revit.Api.RevitAPIUI 2024.3.30, Nice3point.Revit.Toolkit 2024.3.0 (per D-02)
       - net8.0-windows conditional: Nice3point.Revit.Api.RevitAPI 2026.4.0, Nice3point.Revit.Api.RevitAPIUI 2026.4.0, Nice3point.Revit.Toolkit 2026.1.0 (per D-02)

    3. Create `OlympeMaterialManager/OlympeMaterialManager.sln` using `dotnet new sln` then `dotnet sln add`.

    4. Create `OlympeMaterialManager/.gitignore` with entries: bin/, obj/, .vs/, *.user, packages/, *.suo, *.ncrunchproject, *.ncrunchsolution, .idea/

    5. Run `dotnet restore` and then `dotnet build` for both TFMs. If build fails on XAML compilation across TFMs (error MC1000, PresentationBuildTasks, or netstandard), document the error and note that Plan 01 Task 2 should STILL create .addin files even if the fallback is needed. Do NOT spend more than 15 minutes debugging build failures -- the fallback path (Shared Project) is documented in RESEARCH.md lines 677-699.

    CRITICAL per D-04: If any placeholder .cs file is needed to make the build succeed, ensure any ElementId usage uses `.Value` (long), never `.IntegerValue`.
  </action>
  <verify>
    <automated>cd OlympeMaterialManager && dotnet build src/OlympeMaterialManager/OlympeMaterialManager.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj exists
    - File contains string "net48;net8.0-windows" (grep-verifiable)
    - File contains string "CommunityToolkit.Mvvm" with Version="8.4.2"
    - File contains string "PolySharp" with Version="1.15.0"
    - File contains string "Nice3point.Revit.Api.RevitAPI" in at least 2 ItemGroups (one per TFM)
    - File contains string "REVIT2024" and "REVIT2025_OR_GREATER" as DefineConstants
    - File OlympeMaterialManager/OlympeMaterialManager.sln exists and contains "OlympeMaterialManager.csproj"
    - File OlympeMaterialManager/.gitignore exists and contains "bin/" and "obj/"
    - `dotnet build` exits with code 0 (both TFMs succeed)
    - Directory OlympeMaterialManager/src/OlympeMaterialManager/bin/Release/net48/ contains OlympeMaterialManager.dll
    - Directory OlympeMaterialManager/src/OlympeMaterialManager/bin/Release/net8.0-windows/ contains OlympeMaterialManager.dll
    - Neither output directory contains RevitAPI.dll (CopyLocal=false verified per INFRA-02)
  </acceptance_criteria>
  <done>Both TFM builds succeed, NuGet packages restored, no Revit API DLLs copied to output</done>
</task>

<task type="auto">
  <name>Task 2: Create .addin registration files for all three Revit versions</name>
  <files>
    OlympeMaterialManager/addin/OlympeMaterialManager.2024.addin,
    OlympeMaterialManager/addin/OlympeMaterialManager.2025.addin,
    OlympeMaterialManager/addin/OlympeMaterialManager.2026.addin
  </files>
  <read_first>
    .planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md (lines 578-603 for .addin XML format and placement paths),
    .planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md (D-14 for .addin requirements)
  </read_first>
  <action>
    1. Create directory `OlympeMaterialManager/addin/`.

    2. Generate ONE GUID to use across all three .addin files. Use the same AddInId in each file per D-14 and RESEARCH.md guidance. Example GUID format: A1B2C3D4-E5F6-7890-ABCD-EF1234567890 (generate a real unique one).

    3. Create three .addin files, each with this XML structure:
       ```xml
       &lt;?xml version="1.0" encoding="utf-8"?&gt;
       &lt;RevitAddIns&gt;
         &lt;AddIn Type="Application"&gt;
           &lt;Name&gt;Olympe MaterialManager&lt;/Name&gt;
           &lt;Assembly&gt;{PATH_TO_DLL}&lt;/Assembly&gt;
           &lt;AddInId&gt;{SAME_GUID}&lt;/AddInId&gt;
           &lt;FullClassName&gt;Olympe.MaterialManager.App&lt;/FullClassName&gt;
           &lt;VendorId&gt;OLYMPE&lt;/VendorId&gt;
           &lt;VendorDescription&gt;Olympe&lt;/VendorDescription&gt;
         &lt;/AddIn&gt;
       &lt;/RevitAddIns&gt;
       ```

    4. Assembly paths per version (relative from addin/ for dev deployment -- absolute paths for production are handled by WiX in Phase 5):
       - 2024.addin: `Assembly` points to `..\\src\\OlympeMaterialManager\\bin\\Release\\net48\\OlympeMaterialManager.dll`
       - 2025.addin: `Assembly` points to `..\\src\\OlympeMaterialManager\\bin\\Release\\net8.0-windows\\OlympeMaterialManager.dll`
       - 2026.addin: `Assembly` points to `..\\src\\OlympeMaterialManager\\bin\\Release\\net8.0-windows\\OlympeMaterialManager.dll`

    5. The .addin files are for local development. During dev, the user will manually copy/symlink them to:
       - %APPDATA%\Autodesk\Revit\Addins\2024\
       - %APPDATA%\Autodesk\Revit\Addins\2025\
       - %APPDATA%\Autodesk\Revit\Addins\2026\

    NOTE: Use the SAME AddInId GUID in all three files. Different GUIDs would register as separate add-ins.
  </action>
  <verify>
    <automated>ls OlympeMaterialManager/addin/*.addin && grep -c "AddInId" OlympeMaterialManager/addin/OlympeMaterialManager.2024.addin OlympeMaterialManager/addin/OlympeMaterialManager.2025.addin OlympeMaterialManager/addin/OlympeMaterialManager.2026.addin</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist: OlympeMaterialManager/addin/OlympeMaterialManager.2024.addin, .2025.addin, .2026.addin
    - Each file contains valid XML with root element "RevitAddIns"
    - Each file contains "AddIn Type=\"Application\""
    - Each file contains "FullClassName" with value "Olympe.MaterialManager.App"
    - The AddInId GUID is identical across all three files (grep all three and compare)
    - The 2024.addin Assembly path contains "net48"
    - The 2025.addin and 2026.addin Assembly paths contain "net8.0-windows"
    - Each file contains "VendorId" with value "OLYMPE"
  </acceptance_criteria>
  <done>Three .addin files with correct XML structure, same GUID, and correct per-version assembly paths</done>
</task>

</tasks>

<verification>
1. `dotnet build OlympeMaterialManager/src/OlympeMaterialManager/OlympeMaterialManager.csproj -c Release` succeeds for both TFMs
2. No RevitAPI.dll or RevitAPIUI.dll in any output bin/ directory
3. Three .addin files exist with well-formed XML and matching GUIDs
4. .gitignore properly excludes build artifacts
</verification>

<success_criteria>
- Build succeeds for both net48 and net8.0-windows targets
- NuGet packages (CommunityToolkit.Mvvm, PolySharp, Nice3point.Revit.*, Behaviors.Wpf) all restore
- Revit API DLLs are NOT in output (CopyLocal=false)
- .addin files are well-formed and version-specific
- If multi-target build fails, document the exact error for fallback decision
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md`
</output>
