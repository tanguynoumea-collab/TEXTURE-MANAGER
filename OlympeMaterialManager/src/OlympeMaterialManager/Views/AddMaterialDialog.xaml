<Window x:Class="Olympe.MaterialManager.Views.AddMaterialDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Olympe.MaterialManager.Converters"
        Title="Ajouter un matériau au preset"
        Width="400" Height="500"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource OlympeWindowStyle}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OlympeMaterialManager;component/Themes/OlympeTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:ArgbToColorConverter x:Key="ArgbToColorConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0 : Recherche (placeholder overlay, UI-M9) -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <TextBox x:Name="SearchBox"
                     AutomationProperties.Name="Rechercher un matériau"
                     TextChanged="SearchBox_TextChanged" />
            <TextBlock Text="Rechercher un matériau…"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Row 1 : Liste des materiaux -->
        <ListBox x:Name="MaterialList"
                 Grid.Row="1"
                 Background="{StaticResource SurfaceBrush}"
                 BorderThickness="0"
                 SelectionMode="Extended">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <Rectangle Width="14" Height="14" RadiusX="2" RadiusY="2" Margin="0,0,8,0">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{Binding ColorArgb, Converter={StaticResource ArgbToColorConverter}}" />
                            </Rectangle.Fill>
                        </Rectangle>
                        <TextBlock Text="{Binding MaterialName}"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <!-- UI-03: BasedOn added for theme consistency -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource BackgroundBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Row 2 : Groupe cible + boutons -->
        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="Groupe cible :"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,4" />
            <ComboBox x:Name="GroupCombo"
                      DisplayMemberPath="GroupName"
                      Margin="0,0,0,8" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ajouter"
                        Click="Ajouter_Click"
                        Background="{StaticResource AccentBrush}"
                        Margin="0,0,8,0" />
                <Button Content="Annuler"
                        Click="Annuler_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>

</Window>
