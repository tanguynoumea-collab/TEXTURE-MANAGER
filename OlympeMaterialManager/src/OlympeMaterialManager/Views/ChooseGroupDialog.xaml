<Window x:Class="Olympe.MaterialManager.Views.ChooseGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choisir le groupe de destination"
        Width="340" Height="180"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource OlympeWindowStyle}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OlympeMaterialManager;component/Themes/OlympeTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Label -->
        <TextBlock Grid.Row="0"
                   Text="Choisir le groupe de destination :"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,6" />

        <!-- ComboBox de selection de groupe -->
        <ComboBox x:Name="GroupComboBox"
                  Grid.Row="1"
                  DisplayMemberPath="GroupName"
                  Margin="0,0,0,12"
                  VerticalContentAlignment="Center" />

        <!-- Boutons OK / Annuler -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Click="Ok_Click"
                    IsDefault="True"
                    Background="{StaticResource AccentBrush}"
                    MinWidth="70"
                    Margin="0,0,8,0" />
            <Button Content="Annuler"
                    Click="Annuler_Click"
                    IsCancel="True"
                    MinWidth="70" />
        </StackPanel>
    </Grid>

</Window>
